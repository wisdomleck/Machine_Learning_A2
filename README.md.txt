The notebook follows the format.

1. EDA
2. training/exploring individual classifiers
3. training/exploring stacking model
4. generating predictions for the best model

The model contains code to train and create predictions for each model in sequential order. 
You must have the provided datasets unzipped in the working directory.
